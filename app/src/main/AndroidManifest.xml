<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NBA_NAAC_EMS">
        <activity
            android:name=".StudentListActivity"
            android:exported="false" />
        <activity
            android:name=".FacultyListActivity"
            android:exported="false" />
        <activity
            android:name=".EventListActivity"
            android:exported="false" />
        <activity
            android:name=".StudentAchievementActivity"
            android:exported="false" />
        <activity
            android:name=".FacultyAchievementActivity"
            android:exported="false" />
        <activity
            android:name=".EventAchievementActivity"
            android:exported="false" />
        <activity
            android:name=".EvidenceActivity"
            android:exported="false" />
        <activity
            android:name=".DetailActivity"
            android:exported="false" /> <!-- LOGIN -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" /> <!-- SPLASH (START FIRST) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- DASHBOARD -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".ReportActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>